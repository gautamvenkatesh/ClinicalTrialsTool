/**
 * Client to interface with trials backend
 */

import { Trial } from "../typings/Trial";

export function fetchNewTrials(): Array<Trial> {
    return [];
}

export function generalSearch(): Array<Trial> {
    return [];
}